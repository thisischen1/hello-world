# Supabase — free tier is plenty for MVP
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Anthropic — Haiku 4.5 + Sonnet 4.6 with prompt caching, ~$30-80/mo at MVP scale
ANTHROPIC_API_KEY=sk-ant-...

# Per-creator OAuth tokens (one set per onboarded creator)
# Replace <id> with the creator UUID from the creators table.
# TIKTOK_TOKEN_<id>=
# IG_TOKEN_<id>=
# IG_USER_<id>=
